// Phase 04 — the methodology section is now driven by MethodologySequence.
// This file remains as a thin re-export so existing imports keep working
// without touching the landing page composition.
export { MethodologySequence as HowItWorks } from './MethodologySequence'
